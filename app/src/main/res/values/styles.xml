<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionMenuTextAppearance">@style/MenuTextStyle</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MenuTextStyle">
        <item name="android:textSize">20sp</item>
    </style>

    <!--AddCity-->
    <style name="CustomTheme" parent="DefaultCityPickerTheme">
        <item name="cpCancelTextColor">@color/colorGreen</item>
        <item name="cpSearchCursorDrawable">@color/colorGreen</item>
        <item name="cpIndexBarNormalTextColor">@color/colorGreen</item>
        <item name="cpIndexBarSelectedTextColor">@color/colorGreen</item>
        <item name="cpSectionHeight">@dimen/cp_section_height</item>
        <item name="cpOverlayBackground">@color/colorGreen</item>
    </style>

    <style name="CustomAnim">
        <item name="android:windowEnterAnimation">@anim/custom_push_down_in</item>
        <item name="android:windowExitAnimation">@anim/custom_push_up_out</item>
    </style>

</resources>
